services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: youtube-download-app
    ports:
      - "3100:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
